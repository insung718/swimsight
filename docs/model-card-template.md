# Model Card: `<model-version>`

## Status

- Release state: `CHALLENGER | CHAMPION | REJECTED | RETIRED`
- Evaluation state: `VALIDATED | PROVISIONAL | INSUFFICIENT`
- Artifact SHA-256:
- Dataset version:
- Feature schema version:
- Training code commit:
- Evaluation version:

## Intended and excluded use

Describe the supported event, course, population, forecast horizons, and decision-support purpose. State prohibited medical, selection, protected-trait, and guarantee uses.

## Data and provenance

Document source systems, inclusion rules, consent snapshot, cohort sizes, date range, duplicate resolution, identity resolution, conversions, corrections, and exclusions. Do not include athlete identifiers.

## Validation

Report rolling-origin and athlete-group results separately. Include champion and deterministic baselines, MAE, median absolute error, RMSE, interval coverage, Brier score, calibration error, residual quantiles, subgroup sample sizes, and confidence intervals where defined.

## Limitations and failure modes

List sparse histories, distribution boundaries, unmeasured context, source correction limits, low-sample cohorts, and known calibration gaps.

## Release decision

Attach immutable gate output, subgroup guardrails, operator decision, reason, timestamp, and any override. Never rewrite a prior decision.
